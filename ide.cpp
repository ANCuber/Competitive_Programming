#include <bits/stdc++.h>
using namespace std; 

for (int = 1; i < n; ++i) {
    for updated Edges:
        dis[v] = min(dis[v],dis[u]+w[u][v]);
}

int main() {
    int a, b;
    cin>>a>>b;
    cout<<a+b<<endl;
    return 0;
}
