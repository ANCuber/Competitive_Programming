#pragma GCC optimize("Ofast","unroll-loops")
#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pb push_back 

struct Book {
    int title = 0;
};

signed main() {
    cin.tie(0)->sync_with_stdio(0);
    int a, b; cin>>a>>b;
    cout<<1+2<<'\n';
    if (true) {
        int b = 2;
    } else {
        int a = 3;
    }
    cout<<a+b<<'\n';
    return 0;
}
