#include <bits/stdc++.h>
using namespace std;

int main() {
    int n; cin>>n;
    string s; cin>>s;
    bool arr[n];
    for (int i = 0; i < n; ++i) {
        arr[i] = s[i]-'0';
    }
    
}