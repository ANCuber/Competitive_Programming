#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

vector <int> numbers;
int l, r;
int ans[2] = {0,0};

int pick() {

}

int main() {
    int n; cin>>n;
    numbers.resize(n);
    for (int i = 0; i < n; ++i) {
        cin>>numbers[i];
    }
    l = 0, r = n-1;

}