#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'
#define pii pair<int,int>

/*bitset <300000005> p;

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    p.set();
    unsigned ll ans = 0;
    unsigned ll base = 4294967296;
    for (int i = 2; i <= 300000000; ++i) {
        if (p[i]) {
            ans += i;
            ans %= base;
            if (i >= 17321) continue;
            for (int j = i*i; j <= 300000000; j += i) {
                p[j] = 0;
            }
        }
    }
    p[0] = p[1] = 0;
    cout<<p.count()<<endl;
    cout<<ans<<endl;
    return 0;
}*/

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    cout<<"16252325"<<endl;
    cout<<"3100702502"<<endl;
    return 0;
}


