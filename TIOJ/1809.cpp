#include <bits/stdc++.h>
using namespace std;

#define pii pair<int,int>
#define endl '\n'

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int n, m, q;
    cin>>n>>m>>q;
    int a, b;
    while(m--) {
        cin>>a>>b;
        
    }
    return 0;
}
