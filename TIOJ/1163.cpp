#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'
#define pii pair<int,int>

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int v, e; cin>>v>>e;
    int x, y, d;
    for (int i = 0; i < e; ++i) {
        
    }
    int q; cin>>q;
    int st, ed;
    while(q--) {
        cin>>st>>ed;
        
    }
    return 0;
}
