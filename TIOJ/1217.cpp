#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    char arr[2005];
    while(cin.getline(arr,2000)) {
        if (cin.fail()) break;
        cout<<arr<<endl;
    }
    return 0;
}