#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int n, m;
    cin>>n>>m;
    int B[n+1];
    for (int i = 1; i <= n; ++i) {
        cin>>B[i];
    }
    int id = 1;
    vector <int> sta;
    for (int i = 1; i <= n; ++i) {
        
    }
    return 0;
}