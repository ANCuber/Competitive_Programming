#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

void failure(vector<int> &b, string p) {

}

int KMP() {
    
}

int main() {
    int t; cin>>t;
    while(t--) {
        string t; cin>>t;
        int q; cin>>q;
        while(q--) {
            string p; cin>>p;
            vector <int> border(p.size());
            border[0] = -1;
            failure(border,p);

        }       
    }
    return;
}