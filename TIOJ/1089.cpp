#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'
#define pii pair<int,int>

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int n, k; cin>>n>>k;
    int x, y;
    int ans = 0;
    vector<int> r(n+1,0), c(n+1,0);
    for (int i = 0; i < k; ++i) {
        cin>>x>>y;
        
    }
    cout<<ans<<endl;
    return 0;
}
