#include <bits/stdc++.h>
using namespace std;

int main() {
    string record;
    while(1) {
        if (cin.fail()) break;
        cin>>record;
        vector <bool> redteam;
        vector <bool> blueteam;
        int redscore = 0;
        int bluescore = 0;
        for (int i = 0; i < record.size(); i++) {
            
            switch(record[i]) {
                case 'K': case 'O':
                    
                case 'W':
                    
                case 'S':
                    
                case 'D':
                    
                case 'T':
                    
                case 'H':
                    
            }
        }
        cout<<redscore<<' '<<bluescore<<'\n';
    }
    return 0;
}