#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'
#define pii pair<int,int>
#define p_q priority_queue

struct Rock{
    int w, m, c;
};

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int n; cin>>n;
    vector<Rock> r(n);
    for (int i = 0; i < n; ++i) cin>>r[i].w>>r[i].m>>r[i].c;
    int t; cin>>t;
}
