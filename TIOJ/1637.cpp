#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'
#define pii pair<int,int>

int n;
ll ans = 0, sum = 0;
vector<ll> g;

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    cin>>n;
    g.resize(n);
    for (auto& i : g) cin>>i;
    stack <int> stk;
    for (int i = 0; i < n; ++i) {
        
    }
    
    return 0;
}
