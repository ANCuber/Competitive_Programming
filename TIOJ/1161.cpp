#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'
#define pii pair<int,int>

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int t; cin>>t;
    int n, k;
    while(t--) {
        cin>>n>>k;
        int s, a;
        for (int i = 0; i < n; ++i) {
            cin>>s>>a;
            if (i < k) {
                
            } else {
                
            }
        }
    }
    return 0;
}
