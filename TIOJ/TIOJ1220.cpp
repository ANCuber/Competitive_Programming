#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define ll long long

int n, m;

int main() {
    cin>>n>>m;
    vector<int> boss(n+1);
    for (int i = 1; i < n+1; ++i) boss[i] = i;
    int a, b;
    while(m--) {
        cin>>a>>b;
        
    }
}