#include <bits/stdc++.h>
using namespace std;

int main() {
    vector <int> a, b;
    string x, y;
    cin>>x>>y;
    int as = x.size();
    int bs = y.size();

    for (int i = 0; i < as; i++) {
        int k = x[i]-'0';
        a.push_back(k);
    }
    for (int i = 0; i < bs; i++) {
        int k = y[i]-'0';
        b.push_back(k);
    }
    while(1) {
        
    }
}