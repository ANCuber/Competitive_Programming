#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

int main() {
    string A, B;
    cin>>A>>B;
    ll len_a = A.size(), len_b = B.size();
    vector <int> a(len_a);
    vector <int> b(len_b);
    for (int i = 0; i < len_a; ++i) a[i] = A[i]-'0';
    for (int i = 0; i < len_b; ++i) b[i] = b[i]-'0';
    vector <int> ans;
    for (int i = 0; i < len_b; ++i) {
        
    }
}