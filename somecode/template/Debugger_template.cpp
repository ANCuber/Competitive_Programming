#pragma GCC optimize("Ofast","unroll-loops")
#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pb push_back

template<typename T>
void debug(T input){
    cout<<input<<endl;
}

signed main() {
    cin.tie(0)->sync_with_stdio(0);
    int a; cin>>a;
    debug(a);
    string s; cin>>s;
    debug(s);
    return 0;
}
