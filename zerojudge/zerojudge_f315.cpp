#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define ll long long

int main() {
    int n; cin>>n;
    vector <int> arr((n<<1)+1);
    for (int i = 1; i <= (n<<1); ++i) {
        cin>>arr[i];
    }
    ll ans = 0;
    for (int i = 1; i <= n; ++i) {

    }
    return 0;
}