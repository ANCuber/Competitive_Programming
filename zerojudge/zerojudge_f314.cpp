#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define ll long long

int main() {
    int m, n; cin>>m>>n;
    int graph[m][n];
    for (int i = 0;i < m; ++i) {
        for (int j = 0; j < n; ++j) {
            cin>>graph[i][j];
        }
    }
    
    return 0;
}