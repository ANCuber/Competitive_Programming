#pragma GCC optimize("O3")
#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pii pair<int,int>
#define p_q priority_queue
#define endl '\n'
#define pb push_back

int n, k, a;
int dp[201] = {0};
int mx[201] = {0};
ll ans = 0;

signed main() {
    scanf("%d%d",&n,&k);    
    for (int i = 1; i <= n; ++i) {
        scanf("%d",&a);
        for (int i = 1; i <= k; ++i) {
            
        }
    }
    printf("%lld\n",ans);
    return 0;
}
