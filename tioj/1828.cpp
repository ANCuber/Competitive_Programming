#pragma GCC optimize("Ofast")
#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pii pair<int,int>
#define p_q priority_queue
#define endl '\n'
#define pb push_back

const unsigned  int mxd = 1e4;
unsigned int x, y, z;

signed main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int t; cin>>t;    
    while(t--) {
        cin>>x>>y>>z;
        if (x > mxd) cout<<0<<endl;
        else {
            
        }
    }
    
    return 0;
}
