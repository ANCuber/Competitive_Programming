#pragma GCC optimize("Ofast","unroll-loops")
#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pb push_back

signed main() {
    cin.tie(0)->sync_with_stdio(0);
    set<pair<int,int> > s;
    auto iter = s.begin();
    *iter.first
    iter->first

    pair<int,int> p;
    p.first;
    return 0;
}
