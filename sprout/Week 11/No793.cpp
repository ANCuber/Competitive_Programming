#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

int main() {
    int n, k, q;
    cin>>n>>k>>q;
    vector <int> arr(n);
    for (int i = 0; i < n; ++i) cin>>arr[i];
    while(q--) {
        int l, r; cin>>l>>r;
        
    }
    return 0;
}