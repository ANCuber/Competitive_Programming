#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

struct Nd{
    int x;
    int hp;
    int tag = 0;
};

int ql, qr;
int n, m;

int main() {
    cin>>n>>m;
    
    
    int cmd, k;
    while(m--) {
        cin>>cmd;
        cin>>ql>>qr;
        if (cmd == 1) {
            cin>>k;
            
        } else {

        }
    }
    return 0;
}