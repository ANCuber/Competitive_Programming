#include <bits/stdc++.h>
using namespace std;

#define endl '\n'

void Report(int id) {
    cout<<id<<endl;
}

void solve(int n, long long k, long long a[]) {
    
}

int main() {
    int n; cin>>n;
    long long k; cin>>k;
    long long a[n];
    for (int i = 0; i < n; ++i) {
        cin>>a[i];
    }
    solve(n,k,a);
    return 0;
}