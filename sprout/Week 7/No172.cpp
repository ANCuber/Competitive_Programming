#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define mod 100z0007

int main() {
    int t; cin>>t; 
    while(t--) {
        int n, m;
        cin>>n>>m;
        vector <int> amount(n);
        for (int i = 0; i < n; ++i) cin>>amount[i];
        
    }
}