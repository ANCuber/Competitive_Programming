#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m, r;
    cin>>n>>m>>r;
    vector <pair<int,int> > weapon(n);
    for (int i = 0; i < n; ++i) {
        cin>>weapon[i].first>>weapon[i].second;
    }
    
}